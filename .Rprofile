#Set library location to home directory
.libPaths("~/R/libs")

#General options
options(tab.width = 2)
options(editor="vim")

#Set a permanent CRAN Mirror
local({
	r <- getOption("repos")
	r["CRAN"] <- "https://cran.mtu.edu"
	options(repos = r)
})
