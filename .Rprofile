print("Custom .Rprofile loaded")

#Set library location to home directory
.libPaths("~/R/libs")

#Uncomment the following line if colorout is installed
#library(colorout)

#General options
options(tab.width = 2)
options(editor="vim")

#Set a permanent CRAN Mirror
local({
	r <- getOption("repos")
	r["CRAN"] <- "https://cran.mtu.edu"
	options(repos = r)
})

