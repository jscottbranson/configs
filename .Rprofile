#Set library location
.libPaths("~/R/libs")

#General options
options(tab.width = 2)
options(editor="vim")

#Set a perminant CRAN Mirror
local({
	r <- getOption("repos")
	r["CRAN"] <- "https://cran.mtu.edu"
	options(repos = r)
})
